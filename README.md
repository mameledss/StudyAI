# StudyAI
Questo progetto implementa un’applicazione web in Flask che utilizza machine learning (Random Forest) per consigliare il miglior metodo di studio in base alle caratteristiche fornite dall’utente.
